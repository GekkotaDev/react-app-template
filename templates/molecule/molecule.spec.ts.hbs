import { useMolecule } from "bunshi/react";
import { renderHook } from "vitest-browser-react";

import { {{ pascalCase Name }}Molecule } from "./molecule";

describe("{{ {{ pascalCase Name }}Case name }}Molecule", () => {
  const { result } = renderHook(() => useMolecule(NameMolecule));
});
